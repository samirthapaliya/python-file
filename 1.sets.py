a = {1,2,3,4}
b = {3,4,5,6}

print (a.union (b))  #{1 to 6)
print (a.intersection(b)) # {3,4}
print (a.difference(b))  #{1,2}
print (a.difference(b))
print (b.intersection(a))
print (a.symmetric_difference(b))

# create a 3 sets for friuts, with random items, now, i need to get
# data how many items among the sets are reapeted,
#how many of them are unique, in total get all the unrepeated items

help = input("Enter the item")
z=0
for i in r

A = {'apple', 'mango', 'orange', 'grapse'}
B = {'banana','apple','orange','mango'}
C = {'pear','guava','orange','banana'}

repeated = A.intersection(B or C)        #this will show
print("The repeated items are:",repeated)

unique = A.union(B and C )          #this will show all the element of A B and C without repeation
print("The length of the unique item is:",len(unique))
print("The unique fruits are:",unique)


unrepeated = unique.difference(repeated)        #this will show all the items in unique which are not repeated.
print("The unrepeated items are:",unrepeated)








